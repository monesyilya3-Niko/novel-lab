# novel-lab 完善度与能力评估报告

> **日期**：2026-08-07 · **评估方式**：全量静态检查 + 冒烟测试 + 资产校验 + 回归验证
> **项目路径**：`C:\Users\monesy\WorkBuddy\2026-08-06-16-49-41\novel-lab\`
> **结论**：**架构成熟度 ★★★★☆（4.5/5），数据质量 ★★★☆☆（3/5），工具完备度 ★★★★☆（4/5）** —— 系统可运行、可复用、可交付，但写作质量闭环仍受"拆书数据形态"制约。

---

## 一、总览：各项健康度

| 检查项 | 结果 | 状态 |
|---|---|---|
| 16 个 Python 脚本语法 | py_compile 全过 | ✅ |
| CLI 11 个子命令 | --help 冒烟全过 | ✅ |
| 3 份 schema JSON | 全部合法 | ✅ |
| 3 本 voice-card 校验 | 0 硬错误（WARN 可入库） | ✅ |
| 1 份题材包校验 | **修复后 PASS**（原 REJECT） | ✅ 已修 |
| 版权合规扫描 | 3 本资产全 PASS | ✅ |
| 模型配置 | mimo 有密钥，路由正常 | ✅ |
| 拆书报告 | 3 份已产出（可变现） | ✅ |
| 章节产出 | 第 1、2 章已入库 | ✅ |

---

## 二、能力盘点：这套系统现在能做什么

### 已跑通的全链路（5 项能力）

1. **拆书引擎**：TXT → 分层采样（27 章）→ 四遍扫描（结构/人物/文风/商业）→ 归一化 → 3 类资产，含版权合规拦截（12 字原文匹配 REJECT）
2. **题材聚合**：≥3 本同题材 → 题材包（铁律/建议/禁忌/爽点类型），3 本真书已聚合成功
3. **资产驱动写作**：voice-card + 题材包 → 8 节 system prompt → 生成 + 改写循环（目标 ≥90）→ 入库
4. **变现产出**：拆书报告一键生成（闲鱼/公众号可卖），3 份已交付
5. **模型自由**：openai/anthropic/ollama 三协议，11 预设，密钥隔离

### 能力亮点（市面稀缺）
- **商业层分析**：爽点密度、前 3 章逐句拆、付费卡点规划——同类项目零覆盖
- **抽象分级**：structural/scenic/verbal 三级，verbal 拒收 → 法律上可商业化
- **资产版本化**：voice-card / genre-pack / trope-library 三层分离，跨会话复用

---

## 三、本次检查发现并修复的 3 个真实 Bug

| # | 位置 | 问题 | 影响 | 修复 |
|---|---|---|---|---|
| 1 | `validate.py` auto_kind | genre-pack 的 hint 键嵌套在 commercial 里，顶层检测不到 → 误判为 voice-card → REJECT | 题材包无法校验入库 | 按顶层子结构键识别，已 PASS ✅ |
| 2 | `consistency.py` check_voices | 只处理字符串形态 verbal_tics，chireng 的 dict 形态被跳过 | 声线打分完全失效（0/0 核心词，35 分维度白给） | 加 `_vt_to_str` 兼容，修复后能提取核心词 ✅ |
| 3 | `novel.py` CLI 注入 | 缺 `--genre-pack` 参数透传（inject.py 已支持但 CLI 没接） | CLI 无法注入题材包 | 已补上 ✅ |

**额外处理**：state.json 与文件系统不同步（有 2 章但 current_chapter=1）→ 已同步为 2。

---

## 四、已知未解问题（诚实清单，非阻塞）

### 1. 章节一致性实测 72-77，低于 90 目标线 ⚠️
实测第 2 章 vs chireng voice-card = **72.2 分**（非 HANDOFF 记载的 92.4，两者口径不同——92.4 可能是旧版打分器结果）。扣分集中在：
- **声线维度 0/18**：边炀的 verbal_tics 是"规则示例句"（如"我为什么要帮你？"），正文按场景写的是变体（"老子又不是你家保姆"），逐字匹配命中为 0。这是**打分器严格**，但也说明写作时具体词汇指引不足
- **意象维度 5/10**：chireng 的 imagery 数据为空（无比喻取材领域），白丢 5 分

### 2. 拆书数据形态是最大瓶颈 🔧
voice-card 的 `verbal_tics` 存在两种形态（字符串 vs dict 规则），dict 形态里口头禅淹没在规则描述中，`extract_keywords` 提取效率低。**根治方向**：Pass2 prompt 要求口头禅单独提炼为字符串列表，而不是写成"情境+规则示例"。

### 3. 遗留测试文件（无法删除）
`assets/synthetic_book_c-voice-card.json` + `corpus/batch_test/` —— 系统安全删除层拦截 unlink，代码层已过滤 synthetic_*，不影响功能。

---

## 五、能力评估结论

| 维度 | 评分 | 说明 |
|---|---|---|
| 架构设计 | ★★★★★ | 三层解耦 + 资产文件通信，改一处不炸全局 |
| 工程完备 | ★★★★☆ | 16 脚本 + CLI + 3 schema，零第三方依赖拿来即跑 |
| 数据质量 | ★★★☆☆ | 3 本真书资产合规，但 verbal_tics 形态不统一影响下游 |
| 质量闭环 | ★★★☆☆ | 改写循环机制完备，但声线命中率受数据形态制约，实测 72-77 |
| 商业变现 | ★★★★☆ | 拆书报告已可交付，边际成本趋零 |

**一句话结论**：这是一套**架构成熟、链路完整、可立即变现**的拆书系统；要把它从"能拆能写"推到"写得准"，下一步的关键不是写代码，而是**提升 Pass2 拆书输出中口头禅的纯度**（数据形态），以及**在改写循环里给模型更具体的声线词清单**。

---

## 六、建议的下一步（按优先级）

1. **修数据形态**：Pass2 prompt 加"口头禅必须单独用引号列出"约束，重拆 chireng 验证声线命中率提升
2. **题材包注入写第 3 章**：prompt 已生成（`prompts/generated/chireng_genre-writing-prompt.md`），inject.py 已支持 `--genre-pack`，随时可跑
3. **拆第 4 本**（《软柿子》）提升题材包置信度（现 3 本 0.8）
4. M1.6 人工校验 voice-card 准确度

---

*本报告由 novel-lab 健康检查生成，数据基于 2026-08-07 05:54-06:00 实测。*
